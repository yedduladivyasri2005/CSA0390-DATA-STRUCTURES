#include<stdio.h>
#include<conio.h>
int factorial(int);
int main(void)
{
	int n,fact;
	printf("enter a positive number");
	scanf("%d",&n);
	fact=factorial(n);
	printf("factorial of %d=%d",n,fact);
}
int factorial(int n)
{
	if(n==1)
	return 1;
	else
	return n*factorial(n-1);
}
