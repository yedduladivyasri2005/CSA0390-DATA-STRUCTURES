#include<Stdio.h>
int main()
{
   int i,n,fact=1;
   printf("enter a number");
   scanf("%d",&n);
   for(i=1;i<=n;i++)
   fact=fact*i;
   printf("factorial of a given number is %d",fact);
   return 0;
}
